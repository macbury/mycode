<% for status in @statusy %>
	addStatus(<%= status.id %>, '<%= escape_javascript(render(:partial => status)) %>', <%= status.created_at.to_date.to_s(:number) %>, '<%= day_title(status.created_at) %>', false);
<% end %>